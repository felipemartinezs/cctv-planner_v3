## Plan Viewer

Esta capa muestra el mismo plano pagina 1 con:

- zoom y pan
- capa de segmentacion sincronizada
- lista de segmentos

No calcula segmentacion; solo consume `PlanData` y `PlanSegmentation`.
